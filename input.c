#include <stddef.h>#include <stdio.h>#include <stdlib.h>#include <stdbool.h>#include <ctype.h>#include <errno.h>#include "input.h"#include "errors.h"#include "simulation.h"#define power2To32 4294967296int hexToInt(char c) {    if (c >= '0' && c <= '9') {        return c - '0';    } else if (c >= 'a' && c <= 'f') {        return c - 'a' + 10;    } else if (c >= 'A' && c <= 'F') {        return c - 'A' + 10;    }    return -1;}static charArray *scanChar(charArray *array, const char *line, size_t index, int *err) {    char sign;    do {        sign = line[index];        if (isspace(sign) == 0) {            if (hexToInt(sign) == -1) {                *err = 1;                break;            }            pushBackChar(array, sign);        }        index++;    } while (sign != '\n');    return array;}static void scanLine(char **line, int *err, FILE *fptr) {    size_t bufferSize = 0;    size_t lineLength = getline(line, &bufferSize, fptr);    if (errno == ENOMEM) {        free(*line);        *line = NULL;        handleError(0);    }    if (lineLength == (size_t) (-1)) {        *err = 1;        free(*line);        *line = NULL;    }}static void scanToArrayAdd(Array *arr, int *err, char *line) {    size_t i = 0;    size_t number;    if (*err == 1) return;    while (line[i] != '\n' && line[i] != EOF) {        while (isspace(line[i]) && line[i] != '\n' && line[i] != EOF) {            i++;        }        if (isdigit(line[i])) {            number = strtoull(line + i, NULL, 10);            if (errno == ERANGE) {                *err = 1;                break;            }            pushBack(arr, number);        } else if (line[i] == '\n' || line[i] == EOF) {            if (getLength(arr) == 0) {                *err = 1;            }            break;        } else {            *err = 1;            break;        }        while (isdigit(line[i])) i++;    }    if (getLength(arr) == 0) {        *err = 1;    }}static void scanToArray(Array *arr, int *err, FILE *fptr) {    char *line = NULL;    scanLine(&line, err, fptr);    scanToArrayAdd(arr, err, line);    if (line != NULL) {        free(line);        line = NULL;    }}static void convertRToBinary(Array *rests, size_t volume, charArray *bitPositions) {    long long difference;    for (size_t i = 0; i < volume / 4; i++) {        pushBackChar(bitPositions, '0');    }    for (size_t i = 1; i < getLength(rests); i++) {        size_t element = getElementFromArray(rests, i);        difference = (long long) (volume - element);        while (difference >= 0) {            setBit(bitPositions, element);            difference -= power2To32;            element += power2To32;        }    }}static void convertRNumber(Array *numbers, charArray *bitPositions, Array *dimensionArray, int *err) {    if (*err == 1) return;    size_t a, b, m, s_0, r;    size_t product;    a = getElementFromArray(numbers, 0);    b = getElementFromArray(numbers, 1);    m = getElementFromArray(numbers, 2);    r = getElementFromArray(numbers, 3);    s_0 = getElementFromArray(numbers, 4);    if (m == 0) {        *err = 1;        return;    }    Array *s = createArray();    Array *rests = createArray();    pushBack(s, s_0);    for (size_t i = 1; i <= r; i++) {        size_t result = (a * getElementFromArray(s, i - 1) + b) % m;        pushBack(s, result);    }    pushBack(rests, 0);    product = findVolume(dimensionArray);    for (size_t i = 1; i <= r; i++) {        pushBack(rests, getElementFromArray(s, i) % product);    }    size_t volume = findVolume(dimensionArray);    convertRToBinary(rests, volume, bitPositions);    deleteArray(s);    deleteArray(rests);}static void scanHexOrR(charArray *bitPositions, Array *dimensionArray, int *err, FILE *fptr) {    char *line = NULL;    size_t i = 0;    bool hasZero = false;    scanLine(&line, err, fptr);    if (*err == 1) {        free(line);        return;    }    while (isspace(line[i]) || line[i] == '0') {        if (line[i] == '0') {            hasZero = true;        }        i++;    }    if (line[i] == 'R') {        Array *numbers = createArray();        scanToArrayAdd(numbers, err, line + i + 1);        if ((getLength(numbers) != 5) || (checkRInput(numbers, err) == false)) {            deleteArray(numbers);            free(line);            *err = 1;            return;        }        convertRNumber(numbers, bitPositions, dimensionArray, err);        deleteArray(numbers);    } else if (line[i] == 'x' && hasZero) {        i++;        scanChar(bitPositions, line, i, err);        checkVolume(bitPositions, findVolume(dimensionArray), err);    } else if (line[i] == '\n') {        if (getLengthChar(bitPositions) == 0) {            *err = 1;        }    } else {        *err = 1;    }    free(line);}void readInput(Array *dimensionArray, Array *startArray, Array *endArray, charArray *bitPositions, int *err, FILE *fptr) {    scanToArray(dimensionArray, err, fptr);    checkInput(dimensionArray, err);    if (*err == 1) {        handleError(1);        return;    }    scanToArray(startArray, err, fptr);    checkInput(startArray, err);    if (*err == 1) {        handleError(2);        return;    }    scanToArray(endArray, err, fptr);    checkInput(endArray, err);    if (*err == 1) {        handleError(3);        return;    }    checkDimensions(dimensionArray, startArray, endArray, bitPositions, err, fptr);    if (*err == 1) {        handleError(1);        return;    }    scanHexOrR(bitPositions, dimensionArray, err, fptr);    if (*err == 1) {        handleError(4);        return;    }}